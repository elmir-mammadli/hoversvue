import { Theme } from 'cli-highlight';
import { Argv } from 'yargs';
import { FormatData, IOutputFormat } from '../../../output';
export declare type YamlOutputFormatOptions = {
    pretty: boolean;
    highlight: boolean;
};
export declare const DefaultTheme: Theme;
export declare class YamlOutputFormat implements IOutputFormat<YamlOutputFormatOptions> {
    private theme;
    constructor(theme?: Theme);
    registerOptions(options: Argv): Argv<YamlOutputFormatOptions>;
    value<T>(value: T, options: YamlOutputFormatOptions): Promise<string>;
    format(data: FormatData, options: YamlOutputFormatOptions): Promise<string>;
}
//# sourceMappingURL=yaml.d.ts.map