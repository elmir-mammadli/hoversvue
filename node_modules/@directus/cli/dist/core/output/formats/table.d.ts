import { Argv } from 'yargs';
import { FormatData, IOutputFormat } from '../../../output';
import { CLIError } from '../../exceptions';
export declare type TableOutputFormatOptions = {
    ansi: boolean;
    table: string | 'minimal' | 'compact' | 'markdown';
    stacktrace: boolean;
};
export declare class TableOutputFormat implements IOutputFormat<TableOutputFormatOptions> {
    registerOptions(options: Argv): Argv<TableOutputFormatOptions>;
    text(text: string, options: TableOutputFormatOptions): Promise<string>;
    error(error: CLIError, options: TableOutputFormatOptions): Promise<string>;
    format(data: FormatData, options: TableOutputFormatOptions): Promise<string>;
}
//# sourceMappingURL=table.d.ts.map