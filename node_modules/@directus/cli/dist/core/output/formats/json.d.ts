import { Theme } from 'cli-highlight';
import { Argv } from 'yargs';
import { FormatData, IOutputFormat } from '../../../output';
export declare type JsonOutputFormatOptions = {
    pretty: boolean;
    highlight: boolean;
};
export declare const DefaultTheme: Theme;
export declare class JsonOutputFormat implements IOutputFormat<JsonOutputFormatOptions> {
    private theme;
    constructor(theme?: Theme);
    registerOptions(options: Argv): Argv<JsonOutputFormatOptions>;
    value<T>(value: T, options: JsonOutputFormatOptions): Promise<string>;
    format(data: FormatData, options: JsonOutputFormatOptions): Promise<string>;
}
//# sourceMappingURL=json.d.ts.map