import { Argv } from 'yargs';
import { Command } from '../command';
import { IEvents } from '../events';
import { IOptions, Option } from '../options';
export declare type Registrator = (builder: Argv, command: Command, raw: any) => void;
export declare class Options implements IOptions {
    private _raw;
    private _parser;
    private _parsed?;
    private _error?;
    private _registrators;
    constructor(events: IEvents, argv: string[]);
    register(registrator: Registrator): void;
    feature(name: string, registrator: Registrator): void;
    failed(): boolean;
    error(): Error | undefined;
    values(): any;
    list(): Option[];
}
//# sourceMappingURL=options.d.ts.map