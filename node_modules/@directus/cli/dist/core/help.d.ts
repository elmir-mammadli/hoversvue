import { Runtime } from 'gluegun/build/types/runtime/runtime';
import { IOutput } from '../output';
import { Command } from '../command';
import { CommandHelp, GeneralHelp, IHelp } from '../help';
import { IOptions } from '../options';
export declare class Help implements IHelp {
    private entrypoint;
    private options;
    private output;
    private runtime;
    constructor(entrypoint: string, deps: {
        output: IOutput;
        runtime: Runtime;
        options: IOptions;
    });
    suggest(words: string[]): Promise<{
        suggestion: string;
        score: number;
    }[]>;
    getHelp(): Promise<GeneralHelp>;
    displayHelp(): Promise<GeneralHelp>;
    getCommandHelp(command: Command): Promise<CommandHelp>;
    displayCommandHelp(command: Command): Promise<CommandHelp>;
}
//# sourceMappingURL=help.d.ts.map